# dependencies
node_modules/

# build output
dist/
.vite/

# environment — never commit these
.env
.env.local
.env.*.local

# Supabase local state
supabase/.branches/
supabase/.temp/

# editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# local database dumps: these will contain real project data once the
# tracker is in use. Keep them out of a public repo.
*.dump
*.sql.gz
backups/
